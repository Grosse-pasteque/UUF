"""Because python class 'function' doesn't work this will allow you to simule this class and type checking"""
function = "<class 'function'>"
all_types = [int, float, complex, str, tuple, list, set, dict, frozenset]
all_classes = [chr, ord, hex, oct, repr, eval, exec, globals, bin]